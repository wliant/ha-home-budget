<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="005-create-expenses-table" author="claude">
        <comment>Create expenses table for tracking spending transactions</comment>

        <createTable tableName="expenses">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="amount" type="DECIMAL(10,2)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(500)">
                <constraints nullable="false"/>
            </column>

            <column name="expense_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="budget_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="category_id" type="BIGINT"/>

            <column name="created_by" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="version" type="BIGINT" defaultValue="0">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign key to budgets table -->
        <addForeignKeyConstraint
            baseTableName="expenses"
            baseColumnNames="budget_id"
            constraintName="fk_expenses_budget"
            referencedTableName="budgets"
            referencedColumnNames="id"
            onDelete="CASCADE"/>

        <!-- Foreign key to categories table (nullable for uncategorized expenses) -->
        <addForeignKeyConstraint
            baseTableName="expenses"
            baseColumnNames="category_id"
            constraintName="fk_expenses_category"
            referencedTableName="categories"
            referencedColumnNames="id"
            onDelete="RESTRICT"/>

        <!-- Index for finding expenses by budget (frequent query) -->
        <createIndex tableName="expenses" indexName="idx_expenses_budget_id">
            <column name="budget_id"/>
        </createIndex>

        <!-- Index for finding expenses by category (for breakdown queries) -->
        <createIndex tableName="expenses" indexName="idx_expenses_category_id">
            <column name="category_id"/>
        </createIndex>

        <!-- Index for filtering expenses by date range -->
        <createIndex tableName="expenses" indexName="idx_expenses_expense_date">
            <column name="expense_date"/>
        </createIndex>

        <!-- Index for filtering expenses by user -->
        <createIndex tableName="expenses" indexName="idx_expenses_created_by">
            <column name="created_by"/>
        </createIndex>

        <rollback>
            <dropTable tableName="expenses"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
