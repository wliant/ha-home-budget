# API Contracts: Frontend Theme Redesign

No API contract changes required. This feature is a frontend visual-only enhancement.

All existing API endpoints remain unchanged:
- `GET /api/budgets` — Budget listing
- `GET /api/expenses` — Expense listing
- `GET /api/categories` — Category listing
- `GET /api/budgets/dashboard` — Dashboard data

No new endpoints are needed for theme/styling changes.
